# How to Run
1. Clone Git repo to PACE or download file and put on PACE
2. chmod +x extract_network.sh
3. From the directory containing the script: ./extract_network.sh If you get a 'permission denied' or the executable bit didn't stick, you can always run it as: bash extract_network.sh